# Eggshell: Plugin方式と実験計画の正本

2026-09-08。リモートmain `4212b62d3e1a44117a81f71480963a354ceb486f` を土台にする。
本計画は退避したwork-pipe／work-notes計画を置き換える。実装の分担用サブエージェントは使わない。

## 目的と境界

通常の作業をhooksで観測し、Work→Outcomeを自動保存する。生成モデルに別途DTやJSONを
作成させない。検索器は保存済み記録の候補を返す。再利用の判定・共有グラフ抽出・
配信済み本文の省略は既存kernelとPluginが担う。検索類似度を完了の証明にしない。

最終回答前の中断、次の発言、SessionEndで確定したツール結果を捨てない。未確定の
操作や最終回答は作らず、未完了部分を残す。再実行は冪等。保存失敗時にも退避した
確定結果を残し、会話を続行する。`egg drop`を続行の前提にしない。

## マッチャー

仕事の依頼文だけでなく結果も検索対象にする。完了した親の要約と、中断で要約のない
仕事の確定結果を検索できるようにする。現行実装はMiniLM＋字句hybrid。
辞書・字句単独・MiniLM単独の総当たり比較は今回の作業対象から外す。
代替matcherの具体案は未選定であり、その実装・比較試験は本計画に含めない。
局所的な検索試験は候補の到達性と計算時間の確認であり、生成モデルのトークン削減の
証拠にはしない。

## 固定した実験設計

方法・比較対象・採否基準の正本は
[実験プロトコル v1.2](docs/experiment-protocol-v1.md)。
RQ1（総費用と構成要素の効果）、RQ2（変更後の有効性）、
RQ3（履歴・compaction・中断からの継続）を維持する。

比較は機能なしfresh、zgrep/rg、事前索引RAG、作業履歴RAG、文章要約メモリ、A-MEM、
Eggshell単体、原資料RAG＋Eggshellの8方式。機能なしfreshからの削減率を、seed後の
系列全体・各仕事・累積で必ず報告する。Eggshell単体とRAG併用の効果を混同しない。
効果の主張は関連する仕事の24系列に限定する。関連の薄い12系列は誤再利用と
余計な費用の対照とし、そこでの削減は要求しない。関連性は結果を見る前に分類する。
既知のpvclock→fresh TSC→fresh ACPI PMは開発・実務例とし、未使用の仕事系列で
確認試験を行う。RAGの原資料索引と、成長する仕事メモリを分ける。

現在は**確認試験の全対照・全タスクの実行準備が未完了**。開発ブロックは完了した。
初回のpvclock調査はseedとして一度だけ取得し、削減比較には含めない。
比較は2件目のTSCと3件目のACPI PMだけで行う。Fは各比較件を機能なしfreshで実行し、
Pはseedから前の仕事graphを引き継ぐ。seedの物理使用量は別記録に残す。
2026-09-08の実験開始指示を受け、`clock-dev-v2`の再実験ブロックを実施した。
資料・prompt・binary・実行器・rubric・順序を凍結し、モデルなしの実行器検査を通過した上で
Luna/xhighで直列実行した。このブロックだけではRAGへの優位性、最良マッチャー、品質非劣性を判定しない。
確認試験と他の対照は、それぞれの実行前ゲートを満たしてから開始する。
分担用サブエージェントは使わない。

## 現在の証拠

- 中断保存・保存失敗時の退避と自動再試行、結果を含む検索を実装した。
- Lean build、19組の契約試験、実MiniLMでの検索到達性・再索引試験はPASS。
  総トークン削減や品質非劣性を証明するものではない。
- Kconfigの`plugin-return-v1`比較は設計変更で中止。seed 1件のterminal usageは
  995,781 tokens、比較は未完了。優劣を計算しない。
- 過去の実験出力は2026-09-08のユーザー指示で削除済み。
  README等の過去の数値を、現在検証可能な証拠として採用しない。
- `clock-dev-v2`でseedを一度だけ取得し、TSC/ACPI PMの2件目以降をF/Pで比較した。
  seedは9,153,086 tokensで、削減率から除外した。比較はF=10,254,308、P=4,119,781、
  post-seed削減率59.8239%。各ジョブはTSC 63.5972%、ACPI PM 50.3697%。
  これは各1対の開発測定であり、品質評価・RAG比較・統計的主張ではない。
- 現版の既存技術への優位性、変更後の再利用安全性、Main相当の実証は未確認。
